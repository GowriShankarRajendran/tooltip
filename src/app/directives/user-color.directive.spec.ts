import { UserColorDirective } from './user-color.directive';

describe('UserColorDirective', () => {
  it('should create an instance', () => {
    const directive = new UserColorDirective();
    expect(directive).toBeTruthy();
  });
});
